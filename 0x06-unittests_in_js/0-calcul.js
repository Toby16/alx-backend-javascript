/**
 * Basic test with Mocha and Node assertion library
 *
 * calculateNumber - Function to return sum of two numbers
 */

const calculateNumber = (a, b) => Math.round(a) + Math.round(b);

module.exports = calculateNumber;
